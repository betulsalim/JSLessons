let colors = ['kırmızı', 'yeşil'];
let newColors = colors;

colors.push('mavi');
console.log("renkler: " + colors+ "\nYeni renkler: " +newColors);

let information =
{
    name : 'Betül',
    surname : 'Salim',
    birthDate: 2003,
    city : 'İstanbul',
    country : 'Türkiye',
    likeColors : ['purple', 'black']
};

let newInformation = information;
console.log(newInformation);
